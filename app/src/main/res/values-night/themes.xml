<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Picsum" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status, Navigation bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorThemeBackground</item>

        <!-- Custom colors. -->
        <item name="colorAccent">@color/accent</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorThemeBackground">@color/black</item>
        <item name="colorThemeForeground">@color/darkThemeForeground</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="android:overScrollMode">never</item>
    </style>
</resources>